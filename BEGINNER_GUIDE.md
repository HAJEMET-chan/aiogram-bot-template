# Руководство для начинающих - от нуля до понимания

## Что такое Telegram бот? (Простыми словами)

Представьте обычного пользователя Telegram, но вместо человека - это программа (бот). 
Бот может:
- Получать сообщения от пользователей
- Отвечать на сообщения
- Отправлять уведомления
- Показывать кнопки
- Запоминать информацию

**Пример:** Вы пишете боту "Привет", бот отвечает "Привет! Чем могу помочь?"

## Что такое aiogram?

**aiogram** - это библиотека (набор готовых инструментов) для создания Telegram ботов на языке Python.

**Почему aiogram?**
- Современная и популярная
- Поддерживает асинхронность (быстрая работа)
- Хорошая документация
- Активно развивается

## Как работает бот? (Очень просто)

```
1. Пользователь пишет боту сообщение
   ↓
2. Telegram получает это сообщение
   ↓
3. Telegram отправляет сообщение вашему боту (на ваш сервер)
   ↓
4. Ваш бот обрабатывает сообщение (читает, понимает что делать)
   ↓
5. Бот отправляет ответ обратно в Telegram
   ↓
6. Пользователь видит ответ от бота
```

## Два способа получения сообщений

### Способ 1: Polling (опрос) - для начинающих

**Как работает:**
- Ваш бот каждые несколько секунд спрашивает у Telegram: "Есть ли новые сообщения?"
- Если есть - получает их и обрабатывает
- Если нет - ждет и спрашивает снова

**Плюсы:**
- Просто настроить
- Не нужен публичный интернет-адрес
- Легко тестировать на своем компьютере

**Минусы:**
- Менее эффективен
- Медленнее реагирует

**Аналогия:** Вы постоянно проверяете почтовый ящик каждые 5 минут.

### Способ 2: Webhook (вебхук) - для продакшена

**Как работает:**
- Вы даете Telegram адрес вашего сервера
- Когда приходит сообщение, Telegram сам отправляет его на ваш адрес
- Ваш сервер получает и обрабатывает

**Плюсы:**
- Быстрее реагирует
- Более эффективен
- Лучше для большого количества пользователей

**Минусы:**
- Нужен публичный интернет-адрес (HTTPS)
- Сложнее настроить

**Аналогия:** Почтальон сам приносит вам письма, когда они приходят.

## Структура этого шаблона (очень просто)

Представьте, что бот - это ресторан:

```
┌─────────────────────────────────────────┐
│  bot.py - Главный офис ресторана        │
│  (здесь все начинается)                 │
└─────────────────────────────────────────┘
              │
    ┌─────────┼─────────┐
    │         │         │
    ▼         ▼         ▼
┌────────┐ ┌────────┐ ┌────────┐
│ Официант│ │ Повар  │ │ Касса  │
│(handlers)│ │ (БД)   │ │(states)│
└────────┘ └────────┘ └────────┘
```

### bot.py - Главный файл

**Что делает:**
- Запускает бота
- Подключается к базе данных
- Настраивает все компоненты
- Запускает работу

**Аналогия:** Директор ресторана, который открывает его и настраивает все системы.

### handlers/ - Обработчики (официанты)

**Что делает:**
- Получает сообщения от пользователей
- Понимает, что пользователь хочет
- Выполняет нужное действие
- Отправляет ответ

**Пример:**
```python
# Пользователь написал "/start"
# Обработчик получает это сообщение
# Отправляет приветствие
```

**Аналогия:** Официант, который принимает заказ и приносит еду.

### db/ - База данных (склад)

**Что делает:**
- Хранит информацию о пользователях
- Запоминает данные
- Позволяет искать информацию

**Пример:**
- Имя пользователя
- Настройки пользователя
- История сообщений

**Аналогия:** Склад, где хранятся все продукты и записи.

### keyboards/ - Клавиатуры (меню)

**Что делает:**
- Создает кнопки для пользователя
- Позволяет выбирать действия кнопками вместо текста

**Пример:**
Вместо того чтобы писать "Показать профиль", пользователь нажимает кнопку "👤 Профиль"

**Аналогия:** Меню в ресторане с кнопками для заказа.

### states/ - Состояния (этапы заказа)

**Что делает:**
- Запоминает, на каком этапе диалога находится пользователь
- Позволяет делать многошаговые диалоги

**Пример:**
1. Пользователь начал регистрацию
2. Бот ждет имя (состояние: "ожидание имени")
3. Пользователь ввел имя
4. Бот ждет возраст (состояние: "ожидание возраста")

**Аналогия:** Этапы заказа: выбор блюда → выбор напитка → оплата

### filters/ - Фильтры (сортировщики)

**Что делает:**
- Определяет, какой обработчик должен обработать сообщение
- Сортирует сообщения по типам

**Пример:**
- Сообщение из приватного чата → один обработчик
- Сообщение из группы → другой обработчик
- Команда "/start" → обработчик старта

**Аналогия:** Сортировщик писем, который направляет письма в нужный отдел.

### middlewares/ - Промежуточные обработчики

**Что делает:**
- Выполняет действия ДО и ПОСЛЕ обработки сообщения
- Например, логирует все сообщения

**Аналогия:** Система видеонаблюдения, которая записывает все, что происходит.

## Как работает обработка сообщения (пошагово)

Давайте разберем на примере: пользователь написал "/start"

### Шаг 1: Получение сообщения

```
Пользователь → Telegram → Ваш бот
```

### Шаг 2: Middleware (промежуточная обработка)

```python
# Middleware записывает в лог:
# "Получено сообщение от пользователя 123456: /start"
```

### Шаг 3: Фильтрация

```python
# Система проверяет:
# - Это команда? Да, "/start"
# - Из какого чата? Приватный
# - Какое состояние? Нет состояния
# → Найден обработчик для команды /start
```

### Шаг 4: Выполнение обработчика

```python
async def start(msg: types.Message, state: FSMContext) -> None:
    # 1. Получаем информацию о пользователе
    user_id = msg.from_user.id
    user_name = msg.from_user.full_name
    
    # 2. Сохраняем в базу данных (если нужно)
    # await save_user_to_db(user_id, user_name)
    
    # 3. Устанавливаем состояние
    await state.set_state(states.user.UserMainMenu.menu)
    
    # 4. Отправляем ответ
    await msg.answer(f"Привет, {user_name}!")
```

### Шаг 5: Отправка ответа

```
Ваш бот → Telegram → Пользователь видит "Привет, Иван!"
```

### Шаг 6: Логирование (после обработки)

```python
# Middleware записывает в лог:
# "Обработано сообщение за 0.05 секунды"
```

## База данных - что это и зачем?

### Что такое база данных?

**Простыми словами:** Это как Excel таблица, но для программ.

**Пример таблицы пользователей:**

| id | telegram_id | name      | created_at          |
|----|-------------|-----------|---------------------|
| 1  | 123456789   | Иван      | 2024-01-15 10:00:00 |
| 2  | 987654321   | Мария     | 2024-01-16 11:30:00 |

### Зачем нужна база данных?

1. **Запоминать пользователей**
   - Когда пользователь вернется, бот его узнает

2. **Хранить настройки**
   - Язык интерфейса
   - Уведомления включены/выключены

3. **Хранить данные**
   - История заказов
   - Счет пользователя
   - Любая другая информация

### PostgreSQL - что это?

**PostgreSQL** - это программа для работы с базами данных.

**Почему PostgreSQL?**
- Надежная
- Быстрая
- Бесплатная
- Поддерживает сложные запросы

### Как бот работает с базой данных?

```python
# 1. Пользователь написал сообщение
# 2. Бот хочет узнать информацию о пользователе
# 3. Бот отправляет запрос в базу данных:
#    "Дай мне информацию о пользователе с telegram_id = 123456789"
# 4. База данных находит и возвращает данные
# 5. Бот использует эти данные для ответа
```

## Репозитории - что это и зачем?

### Что такое репозиторий?

**Простыми словами:** Репозиторий - это "посредник" между вашим кодом и базой данных.

**Аналогия:** Представьте библиотеку:
- **База данных** = склад с книгами (данные)
- **Репозиторий** = библиотекарь (знает, где что лежит, как найти)
- **Ваш код** = читатель (просто просит книгу)

Вместо того чтобы самому идти на склад и искать книгу, вы просите библиотекаря, и он приносит нужное.

### Зачем нужны репозитории?

**Без репозитория (плохо):**
```python
# В каждом обработчике пишете SQL запросы напрямую
async def handler(msg: types.Message, db_conn) -> None:
    # Получить пользователя
    result = await db_conn._fetchrow(
        "SELECT * FROM users WHERE telegram_id = $1",
        (msg.from_user.id,)
    )
    
    # Обновить счетчик
    await db_conn._execute(
        "UPDATE users SET message_count = message_count + 1 WHERE telegram_id = $1",
        (msg.from_user.id,)
    )
    
    # Получить статистику
    stats = await db_conn._fetchrow(
        "SELECT message_count, created_at FROM users WHERE telegram_id = $1",
        (msg.from_user.id,)
    )
```

**Проблемы:**
- SQL запросы разбросаны по всему коду
- Если изменилась структура таблицы, нужно менять код везде
- Легко ошибиться в SQL запросе
- Сложно тестировать

**С репозиторием (хорошо):**
```python
# Создаете репозиторий один раз
class UserRepository:
    async def get_by_telegram_id(self, telegram_id: int) -> dict | None:
        # Вся логика работы с БД в одном месте
        result = await self.db_conn._fetchrow(
            "SELECT * FROM users WHERE telegram_id = $1",
            (telegram_id,)
        )
        return result.data
    
    async def increment_message_count(self, telegram_id: int) -> None:
        await self.db_conn._execute(
            "UPDATE users SET message_count = message_count + 1 WHERE telegram_id = $1",
            (telegram_id,)
        )

# В обработчике просто используете методы
async def handler(msg: types.Message, user_repo: UserRepository) -> None:
    user = await user_repo.get_by_telegram_id(msg.from_user.id)
    await user_repo.increment_message_count(msg.from_user.id)
```

**Преимущества:**
- Вся работа с БД в одном месте
- Легко изменить логику (меняете только репозиторий)
- Легко тестировать (можно заменить репозиторий на "фейковый")
- Код обработчиков чистый и понятный

### Как работает репозиторий?

**Структура:**

```python
# db/repositories/user_repository.py

class UserRepository:
    def __init__(self, db_conn: PostgresConnection) -> None:
        # Сохраняем соединение с БД
        self.db_conn = db_conn
    
    # Метод для получения пользователя
    async def get_by_telegram_id(self, telegram_id: int) -> dict | None:
        result = await self.db_conn._fetchrow(
            "SELECT * FROM users WHERE telegram_id = $1",
            (telegram_id,)
        )
        return result.data
    
    # Метод для создания пользователя
    async def create(self, telegram_id: int, name: str) -> dict:
        await self.db_conn._execute(
            "INSERT INTO users (telegram_id, name) VALUES ($1, $2)",
            (telegram_id, name)
        )
        # Возвращаем созданного пользователя
        return await self.get_by_telegram_id(telegram_id)
    
    # Метод для обновления
    async def update_name(self, telegram_id: int, new_name: str) -> None:
        await self.db_conn._execute(
            "UPDATE users SET name = $1 WHERE telegram_id = $2",
            (new_name, telegram_id)
        )
```

### Как использовать репозиторий в обработчике?

**Шаг 1: Создаете репозиторий**

```python
# В обработчике
async def handler(
    msg: types.Message,
    db_conn: PostgresConnection,  # Получаем соединение с БД
) -> None:
    # Создаем репозиторий
    user_repo = UserRepository(db_conn)
    
    # Используем методы репозитория
    user = await user_repo.get_by_telegram_id(msg.from_user.id)
    
    if user is None:
        # Создаем нового пользователя
        user = await user_repo.create(
            telegram_id=msg.from_user.id,
            name=msg.from_user.full_name
        )
    
    await msg.answer(f"Привет, {user['name']}!")
```

### Примеры методов репозитория

**Базовые операции:**

```python
class UserRepository:
    # Получить одного пользователя
    async def get_by_id(self, user_id: int) -> dict | None:
        result = await self.db_conn._fetchrow(
            "SELECT * FROM users WHERE id = $1",
            (user_id,)
        )
        return result.data
    
    # Получить всех пользователей
    async def get_all(self) -> list[dict]:
        results = await self.db_conn._fetch(
            "SELECT * FROM users ORDER BY created_at DESC"
        )
        return results.data
    
    # Получить пользователей с фильтром
    async def get_active_users(self) -> list[dict]:
        results = await self.db_conn._fetch(
            "SELECT * FROM users WHERE active = $1",
            (True,)
        )
        return results.data
    
    # Создать пользователя
    async def create(self, telegram_id: int, name: str) -> dict:
        await self.db_conn._execute(
            "INSERT INTO users (telegram_id, name) VALUES ($1, $2)",
            (telegram_id, name)
        )
        return await self.get_by_telegram_id(telegram_id)
    
    # Обновить пользователя
    async def update(self, telegram_id: int, **kwargs) -> None:
        # kwargs = {"name": "Новое имя", "age": 25}
        set_clause = ", ".join([f"{k} = ${i+2}" for i, k in enumerate(kwargs.keys())])
        values = list(kwargs.values()) + [telegram_id]
        
        await self.db_conn._execute(
            f"UPDATE users SET {set_clause} WHERE telegram_id = $1",
            tuple(values)
        )
    
    # Удалить пользователя
    async def delete(self, telegram_id: int) -> None:
        await self.db_conn._execute(
            "DELETE FROM users WHERE telegram_id = $1",
            (telegram_id,)
        )
```

### Где хранятся репозитории?

**Структура папок:**

```
bowling_bot/
└── db/
    └── repositories/          # Здесь хранятся репозитории
        ├── __init__.py
        ├── user_repository.py  # Репозиторий для пользователей
        ├── order_repository.py # Репозиторий для заказов
        └── product_repository.py # Репозиторий для товаров
```

**Правило:** Один репозиторий = одна сущность (пользователь, заказ, товар и т.д.)

### Полный пример использования

**1. Создаем репозиторий:**

```python
# db/repositories/user_repository.py
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from bowling_bot.db.db_api.storages.postgres import PostgresConnection

class UserRepository:
    def __init__(self, db_conn: "PostgresConnection") -> None:
        self.db_conn = db_conn
    
    async def get_or_create(self, telegram_id: int, name: str) -> dict:
        """Получить пользователя или создать нового"""
        # Пытаемся найти
        result = await self.db_conn._fetchrow(
            "SELECT * FROM users WHERE telegram_id = $1",
            (telegram_id,)
        )
        
        if result.data:
            return result.data
        
        # Создаем нового
        await self.db_conn._execute(
            "INSERT INTO users (telegram_id, name) VALUES ($1, $2)",
            (telegram_id, name)
        )
        
        # Возвращаем созданного
        result = await self.db_conn._fetchrow(
            "SELECT * FROM users WHERE telegram_id = $1",
            (telegram_id,)
        )
        return result.data
```

**2. Используем в обработчике:**

```python
# handlers/user/start.py
from typing import TYPE_CHECKING
from aiogram import types
from aiogram.fsm.context import FSMContext

if TYPE_CHECKING:
    from bowling_bot.db.db_api.storages.postgres import PostgresConnection
    from bowling_bot.db.repositories.user_repository import UserRepository

async def start(
    msg: types.Message,
    state: FSMContext,
    db_conn: "PostgresConnection",
) -> None:
    if msg.from_user is None:
        return
    
    # Создаем репозиторий
    user_repo = UserRepository(db_conn)
    
    # Используем метод репозитория
    user = await user_repo.get_or_create(
        telegram_id=msg.from_user.id,
        name=msg.from_user.full_name or "Не указано"
    )
    
    await msg.answer(f"Привет, {user['name']}!")
```

### Преимущества репозиториев

1. **Организация кода**
   - Вся работа с БД в одном месте
   - Легко найти и изменить

2. **Переиспользование**
   - Один раз написали метод - используете везде
   - Не нужно дублировать SQL запросы

3. **Тестирование**
   - Легко создать "фейковый" репозиторий для тестов
   - Не нужно подключаться к реальной БД

4. **Безопасность**
   - SQL запросы в одном месте
   - Легче проверить на ошибки
   - Можно добавить валидацию

5. **Гибкость**
   - Легко изменить логику работы с БД
   - Можно добавить кэширование
   - Можно добавить логирование

### Когда создавать новый репозиторий?

**Создавайте отдельный репозиторий для:**
- Каждой основной сущности (User, Order, Product)
- Группы связанных операций

**Примеры:**
```python
UserRepository      # Работа с пользователями
OrderRepository     # Работа с заказами
ProductRepository   # Работа с товарами
PaymentRepository   # Работа с платежами
```

### Резюме

**Репозиторий - это:**
- Класс, который содержит методы для работы с БД
- "Посредник" между вашим кодом и базой данных
- Способ организовать работу с данными

**Используйте репозитории для:**
- Организации кода
- Переиспользования логики
- Упрощения тестирования
- Улучшения читаемости кода

**Помните:** Один репозиторий = одна сущность (пользователь, заказ и т.д.)

## Redis - что это и зачем?

### Что такое Redis?

**Redis** - это очень быстрая база данных в памяти.

**Простыми словами:** Как обычная база данных, но данные хранятся в оперативной памяти (RAM), а не на диске. Поэтому очень быстро.

### Зачем Redis в этом шаблоне?

1. **Хранение состояний FSM**
   - Когда пользователь в процессе диалога, его состояние хранится в Redis
   - Быстрый доступ к состоянию

2. **Кэширование (опционально)**
   - Хранение часто используемых данных
   - Чтобы не обращаться к PostgreSQL каждый раз

**Пример:**
```python
# Вместо того чтобы каждый раз спрашивать у PostgreSQL:
# "Какой язык у пользователя?"
# Бот спрашивает один раз, сохраняет в Redis на 1 час
# И следующие запросы идут в Redis (быстрее)
```

## FSM (Finite State Machine) - что это?

### Простыми словами

**FSM** - это способ запомнить, на каком этапе диалога находится пользователь.

### Пример из жизни

Вы звоните в службу поддержки:

1. **Состояние 1:** Оператор спрашивает "Чем могу помочь?"
2. **Состояние 2:** Вы говорите проблему, оператор спрашивает "Какой у вас номер договора?"
3. **Состояние 3:** Вы называете номер, оператор спрашивает "Подтвердите, что это вы?"
4. **Состояние 4:** Вы подтверждаете, оператор решает проблему

Каждое из этих - это **состояние**.

### Пример в боте

**Регистрация пользователя:**

```python
# Состояние 1: Ожидание имени
# Пользователь видит: "Введите ваше имя:"
# Бот ждет текстовое сообщение

# Состояние 2: Ожидание возраста  
# Пользователь ввел имя
# Бот видит: "Введите ваш возраст:"
# Бот ждет число

# Состояние 3: Подтверждение
# Пользователь ввел возраст
# Бот показывает: "Имя: Иван, Возраст: 25. Подтвердите?"
# Бот ждет кнопку "Подтвердить" или "Отмена"
```

### Как это работает в коде?

```python
# 1. Устанавливаем состояние
await state.set_state(states.user.Registration.waiting_name)

# 2. Пользователь вводит имя
# Обработчик получает сообщение и проверяет состояние
# Если состояние = "waiting_name", то это имя

# 3. Сохраняем имя и переходим к следующему состоянию
await state.update_data(name=msg.text)
await state.set_state(states.user.Registration.waiting_age)

# 4. Когда все готово, очищаем состояние
await state.clear()
```

## Клавиатуры - подробно

### Зачем нужны клавиатуры?

**Без клавиатуры:**
```
Пользователь должен писать: "Показать профиль"
Пользователь должен писать: "Настройки"
Пользователь должен писать: "Помощь"
```

**С клавиатурой:**
```
[👤 Профиль]  [⚙️ Настройки]
[❓ Помощь]
```

Намного удобнее!

### Два типа клавиатур

#### 1. Reply-клавиатура (обычные кнопки)

**Как выглядит:**
Кнопки появляются вместо клавиатуры телефона.

**Пример:**
```
┌─────────────┐  ┌─────────────┐
│ 👤 Профиль  │  │ ⚙️ Настройки│
└─────────────┘  └─────────────┘
┌─────────────┐
│ ❓ Помощь   │
└─────────────┘
```

**Когда использовать:**
- Главное меню
- Выбор из списка
- Подтверждение действий

#### 2. Inline-клавиатура (кнопки под сообщением)

**Как выглядит:**
Кнопки появляются прямо под сообщением.

**Пример:**
```
Сообщение от бота:
"Выберите действие:"

[👤 Профиль] [⚙️ Настройки]
[❓ Помощь]
```

**Когда использовать:**
- Выбор опций
- Пагинация (листание страниц)
- Подтверждение без изменения клавиатуры

### Как создаются клавиатуры?

**Простая клавиатура:**

```python
# 1. Определяем схему (сколько кнопок в каждой строке)
schema = [2, 1]  # Первая строка: 2 кнопки, вторая: 1 кнопка

# 2. Определяем текст кнопок
btns = ["Кнопка 1", "Кнопка 2", "Кнопка 3"]

# 3. Создаем клавиатуру
keyboard = MyButtons._create_kb(btns, schema)

# 4. Отправляем с клавиатурой
await msg.answer("Выберите:", reply_markup=keyboard)
```

**Результат:**
```
┌──────────┐  ┌──────────┐
│ Кнопка 1 │  │ Кнопка 2 │
└──────────┘  └──────────┘
┌──────────┐
│ Кнопка 3 │
└──────────┘
```

## Логирование - что это и зачем?

### Что такое логирование?

**Простыми словами:** Запись всего, что происходит с ботом, в файл или консоль.

**Пример лога:**
```
2024-01-15 10:30:15 INFO Получено сообщение от пользователя 123456789
2024-01-15 10:30:15 DEBUG Обработчик: start
2024-01-15 10:30:15 INFO Отправлен ответ пользователю 123456789
2024-01-15 10:30:15 INFO Обработка завершена за 0.05 секунды
```

### Зачем нужно логирование?

1. **Отладка (поиск ошибок)**
   - Если что-то не работает, смотрите логи
   - Видите, что происходило перед ошибкой

2. **Мониторинг (наблюдение)**
   - Сколько пользователей используют бота
   - Какие команды популярны
   - Есть ли ошибки

3. **Аналитика**
   - Когда пользователи активны
   - Какие функции используются чаще

### Что логируется в этом шаблоне?

1. **Все сообщения от пользователей**
   - Кто написал
   - Что написал
   - Когда

2. **Все ответы бота**
   - Что отправил
   - Кому

3. **Запросы к базе данных**
   - Какой запрос
   - Сколько времени занял
   - Была ли ошибка

4. **Ошибки**
   - Что пошло не так
   - Где произошло
   - Подробности

## Переменные окружения (.env файл)

### Что это?

**Переменные окружения** - это настройки, которые хранятся отдельно от кода.

**Почему отдельно?**
- Не нужно менять код для разных настроек
- Можно скрыть секретные данные (пароли)
- Легко переключаться между разработкой и продакшеном

### Что хранится в .env?

```env
# Токен бота (секретный ключ для доступа к боту)
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Настройки базы данных PostgreSQL
PG_HOST=localhost        # Где находится база данных
PG_PORT=5432            # Порт базы данных
PG_USER=postgres        # Имя пользователя
PG_PASSWORD=password    # Пароль
PG_DATABASE=bot_db      # Название базы данных

# Настройки Redis (для состояний)
FSM_HOST=127.0.0.1      # Где находится Redis
FSM_PORT=6379          # Порт Redis
FSM_PASSWORD=          # Пароль (если есть)

# Дополнительные настройки
USE_WEBHOOK=false       # Использовать webhook? (false = polling)
LOGGING_LEVEL=10        # Уровень логирования (10 = DEBUG)
```

### Как это работает?

```python
# В коде:
from bowling_bot.data import config

# Используем настройки:
bot_token = config.BOT_TOKEN  # Читается из .env
db_host = config.PG_HOST      # Читается из .env
```

## Как все работает вместе? (Полный пример)

Давайте разберем полный пример: пользователь регистрируется в боте.

### Шаг 1: Пользователь пишет "/start"

```
Пользователь → Telegram → Ваш бот
```

### Шаг 2: Middleware логирует

```python
# В лог записывается:
# "Получено сообщение: /start от пользователя 123456789"
```

### Шаг 3: Фильтр находит обработчик

```python
# Система проверяет:
# - Это команда "/start"? Да
# - Из приватного чата? Да
# → Найден обработчик start()
```

### Шаг 4: Обработчик выполняется

```python
async def start(
    msg: types.Message,
    state: FSMContext,
    db_conn: PostgresConnection,  # Получаем соединение с БД
) -> None:
    # 1. Получаем информацию о пользователе
    user_id = msg.from_user.id  # 123456789
    user_name = msg.from_user.full_name  # "Иван Иванов"
    
    # 2. Создаем репозиторий для работы с пользователями
    user_repo = UserRepository(db_conn)
    
    # 3. Получаем или создаем пользователя (используем метод репозитория)
    user = await user_repo.get_or_create(
        telegram_id=user_id,
        name=user_name
    )
    
    # 4. Устанавливаем состояние "в главном меню"
    await state.set_state(states.user.UserMainMenu.menu)
    
    # 5. Создаем клавиатуру
    keyboard = MenuButtons.main_menu()
    
    # 6. Отправляем приветствие
    await msg.answer(
        f"Привет, {user['name']}! Добро пожаловать!",
        reply_markup=keyboard
    )
```

### Шаг 5: Пользователь нажимает кнопку "📝 Регистрация"

```
Пользователь нажимает кнопку → Telegram → Ваш бот
```

### Шаг 6: Фильтр находит обработчик

```python
# Система проверяет:
# - Текст сообщения = "📝 Регистрация"? Да
# - Состояние = "menu"? Да
# → Найден обработчик start_registration()
```

### Шаг 7: Обработчик регистрации

```python
async def start_registration(msg: types.Message, state: FSMContext) -> None:
    # 1. Устанавливаем состояние "ожидание имени"
    await state.set_state(states.user.Registration.waiting_name)
    
    # 2. Создаем клавиатуру с кнопкой "Отмена"
    keyboard = BasicButtons.cancel()
    
    # 3. Просим ввести имя
    await msg.answer(
        "Давайте зарегистрируем вас!\nВведите ваше имя:",
        reply_markup=keyboard
    )
```

### Шаг 8: Пользователь вводит имя "Иван"

```
Пользователь → "Иван" → Telegram → Ваш бот
```

### Шаг 9: Фильтр находит обработчик

```python
# Система проверяет:
# - Состояние = "waiting_name"? Да
# - Это текстовое сообщение? Да
# → Найден обработчик process_name()
```

### Шаг 10: Обработчик имени

```python
async def process_name(msg: types.Message, state: FSMContext) -> None:
    # 1. Получаем имя из сообщения
    name = msg.text  # "Иван"
    
    # 2. Сохраняем имя в состояние
    await state.update_data(name=name)
    
    # 3. Переходим к следующему состоянию
    await state.set_state(states.user.Registration.waiting_age)
    
    # 4. Просим ввести возраст
    await msg.answer("Отлично! Теперь введите ваш возраст:")
```

### Шаг 11: Пользователь вводит возраст "25"

### Шаг 12: Обработчик возраста

```python
async def process_age(msg: types.Message, state: FSMContext) -> None:
    # 1. Проверяем, что это число
    try:
        age = int(msg.text)  # 25
        if age < 0 or age > 150:
            raise ValueError
    except ValueError:
        await msg.answer("Пожалуйста, введите корректный возраст (0-150).")
        return
    
    # 2. Сохраняем возраст
    await state.update_data(age=age)
    
    # 3. Получаем все данные
    data = await state.get_data()
    # data = {"name": "Иван", "age": 25}
    
    # 4. Переходим к подтверждению
    await state.set_state(states.user.Registration.waiting_confirmation)
    
    # 5. Показываем данные для подтверждения
    keyboard = BasicButtons.confirmation(add_cancel=True)
    await msg.answer(
        f"Проверьте данные:\n"
        f"Имя: {data['name']}\n"
        f"Возраст: {data['age']}",
        reply_markup=keyboard
    )
```

### Шаг 13: Пользователь нажимает "✅Подтвердить"

### Шаг 14: Обработчик подтверждения

```python
async def confirm_registration(msg: types.Message, state: FSMContext) -> None:
    # 1. Получаем все данные
    data = await state.get_data()
    
    # 2. Сохраняем в базу данных
    await db_conn._execute(
        """
        UPDATE users 
        SET name = $1, age = $2, registered_at = NOW()
        WHERE telegram_id = $3
        """,
        (data['name'], data['age'], msg.from_user.id)
    )
    
    # 3. Очищаем состояние (регистрация завершена)
    await state.clear()
    
    # 4. Возвращаемся в главное меню
    keyboard = MenuButtons.main_menu()
    await msg.answer(
        "Регистрация завершена!",
        reply_markup=keyboard
    )
```

### Шаг 15: Готово!

Пользователь зарегистрирован, данные сохранены в базе, состояние очищено.

## Основные понятия (словарь)

### Асинхронность (async/await)

**Что это?**
Способ выполнения задач, когда программа не ждет завершения одной задачи, а может делать другие.

**Простыми словами:**
Вместо того чтобы ждать, пока загрузится файл, программа может обработать другое сообщение.

**Пример:**
```python
# Обычный код (синхронный):
file = download_file()  # Ждем 5 секунд
process_file(file)       # Только потом обрабатываем

# Асинхронный код:
file = await download_file()  # Начинаем загрузку
# Пока файл загружается, можем обработать другие сообщения
process_file(file)             # Когда загрузится, обрабатываем
```

### Callback (обратный вызов)

**Что это?**
Действие, которое выполняется при нажатии на inline-кнопку.

**Пример:**
```python
# Пользователь нажимает кнопку [Удалить]
# Выполняется функция handle_delete()
```

### Dispatcher (диспетчер)

**Что это?**
Главный компонент aiogram, который распределяет сообщения по обработчикам.

**Аналогия:** Диспетчер такси, который направляет заказы водителям.

### Handler (обработчик)

**Что это?**
Функция, которая обрабатывает определенный тип сообщений.

**Пример:**
```python
async def start_handler(msg: types.Message) -> None:
    # Эта функция обрабатывает команду /start
    await msg.answer("Привет!")
```

### Middleware (промежуточное ПО)

**Что это?**
Код, который выполняется ДО и ПОСЛЕ обработчика.

**Пример:**
- Логирование всех сообщений
- Проверка прав доступа
- Измерение времени обработки

### Router (маршрутизатор)

**Что это?**
Группа обработчиков, объединенных по какому-то признаку.

**Пример:**
- `user_router` - обработчики для обычных пользователей
- `admin_router` - обработчики для администраторов

### State (состояние)

**Что это?**
Текущий этап диалога с пользователем.

**Пример:**
- `menu` - пользователь в главном меню
- `waiting_name` - бот ждет имя пользователя
- `waiting_age` - бот ждет возраст

### Storage (хранилище)

**Что это?**
Место, где хранятся состояния пользователей.

**В этом шаблоне:** Redis используется как storage для FSM.

## Частые вопросы

### Вопрос 1: Зачем нужна база данных, если можно просто хранить в переменных?

**Ответ:**
Переменные хранятся в оперативной памяти. Когда бот перезапускается, все данные теряются. База данных хранит данные на диске, они сохраняются даже после перезапуска.

**Пример:**
```python
# Без БД:
users = {}  # Словарь в памяти
# Перезапустили бота → users = {} (пусто!)

# С БД:
# Данные в PostgreSQL
# Перезапустили бота → данные остались!
```

### Вопрос 2: Почему два Redis? (FSM и Cache)

**Ответ:**
Можно использовать один Redis для обеих целей, но разделение помогает:
- Лучше организовать данные
- Легче управлять
- Можно использовать разные настройки

**На практике:** Часто используют один Redis, но с разными базами данных (db=0 для FSM, db=1 для cache).

### Вопрос 3: Что такое connection pooling?

**Ответ:**
Вместо создания нового соединения с БД для каждого запроса, создается "пул" (набор) готовых соединений. Когда нужен запрос, берется соединение из пула, используется, и возвращается обратно.

**Аналогия:** Такси-парк. Вместо того чтобы каждый раз вызывать новое такси, у вас есть парк машин, которые можно использовать.

**Плюсы:**
- Быстрее (не нужно создавать соединение каждый раз)
- Эффективнее (переиспользование ресурсов)

### Вопрос 4: Зачем нужен SmartAiogramAiohttpSession?

**Ответ:**
Обычная сессия aiogram при ошибке API просто выбрасывает исключение. Smart сессия:
- Автоматически повторяет запрос при временных ошибках
- Ждет нужное время при rate limit
- Логирует все запросы

**Пример:**
```python
# Обычная сессия:
# Ошибка → исключение → бот падает

# Smart сессия:
# Ошибка → повтор через 2 секунды → успех → продолжает работу
```

### Вопрос 5: Что такое Pydantic модели?

**Ответ:**
Pydantic - библиотека для валидации данных. Модели гарантируют, что данные имеют правильный тип и формат.

**Пример:**
```python
class User(BaseModel):
    id: int
    name: str
    age: int

# Если попытаться создать с неправильными данными:
user = User(id="не число", name=123, age="не число")
# Pydantic выбросит ошибку валидации
```

### Вопрос 6: Зачем нужны фильтры?

**Ответ:**
Фильтры определяют, какой обработчик должен обработать сообщение. Без фильтров пришлось бы в каждом обработчике проверять условия.

**Пример:**
```python
# Без фильтров:
async def handler(msg: types.Message) -> None:
    if msg.chat.type != "private":
        return  # Не обрабатываем
    if msg.text != "/start":
        return  # Не обрабатываем
    # Обработка...

# С фильтрами:
user_router.message.filter(ChatTypeFilter("private"))
user_router.message.register(handler, Command("start"))
# Код обработчика чистый, без проверок
```

## Следующие шаги

1. **Прочитайте ARCHITECTURE.md** - подробное описание архитектуры
2. **Изучите USAGE_GUIDE.md** - практические примеры
3. **Используйте CHEATSHEET.md** - быстрая справка
4. **Посмотрите примеры кода** в папке `handlers/`
5. **Попробуйте создать свой обработчик**

## Заключение

Теперь вы понимаете:
- ✅ Что такое Telegram бот
- ✅ Как работает aiogram
- ✅ Структуру этого шаблона
- ✅ Как обрабатываются сообщения
- ✅ Зачем нужна база данных
- ✅ Что такое FSM и состояния
- ✅ Как работают клавиатуры
- ✅ Зачем нужно логирование

**Готовы начать разработку?** Откройте USAGE_GUIDE.md и следуйте примерам!

